# Entorno - Backend API
# Copiar este archivo a ".env" y completar valores reales en local/producción

# Servidor
PORT=3000
NODE_ENV=development
CORS_ORIGIN=*

# URL base del frontend (para enlaces en WhatsApp)
APP_BASE_URL=http://localhost:5173

# JWT
JWT_SECRET=changeme-super-secret
JWT_EXPIRES_IN=24h

# Base de datos (Neon PostgreSQL)
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# WhatsApp Cloud API (Meta)
WHATSAPP_TOKEN=your_meta_access_token
WHATSAPP_PHONE_ID=your_phone_number_id
WHATSAPP_BASE_URL=https://graph.facebook.com/v18.0
# Intentar enviar un template antes del texto para abrir la ventana de 24h
WA_TRY_TEMPLATE_BEFORE_TEXT=true
# Nombre y lenguaje del template a usar (debe existir y estar aprobado en tu WABA)
WA_TEMPLATE_NAME=hello_world
WA_TEMPLATE_LANG=en_US

# Rutas de desarrollo (para pruebas locales)
ENABLE_DEV_ROUTES=true